"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import { FiArrowLeft, FiArrowRight, FiCheck, FiEye, FiEyeOff } from "react-icons/fi";
import { ParentPhotoSlider } from "@/components/parent-photo-slider";
import { ProfilePhotoEditor } from "@/components/profile-photo-editor";
import { apiBase, saveTeacherSession, type TeacherSession } from "@/lib/session";

const steps = ["Your details", "Contact", "Security"];
type Props = { mode: "login" | "signup" };
type Values = { title: "" | "Aunty" | "Uncle"; firstName: string; lastName: string; birthDate: string; whatsappNumber: string; mobileNumber: string; email: string; residentialAddress: string; password: string; confirmPassword: string; profilePhoto: File | null };
const initial: Values = { title: "", firstName: "", lastName: "", birthDate: "", whatsappNumber: "", mobileNumber: "", email: "", residentialAddress: "", password: "", confirmPassword: "", profilePhoto: null };

export function TeacherAuth({ mode }: Props) {
  const signup = mode === "signup";
  const [step, setStep] = useState(0); const [values, setValues] = useState<Values>(initial);
  const [login, setLogin] = useState({ identifier: "", password: "" }); const [showPassword, setShowPassword] = useState(false);
  const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [verificationUrl, setVerificationUrl] = useState(""); const [error, setError] = useState("");
  const update = <K extends keyof Values>(key: K, value: Values[K]) => setValues((current) => ({ ...current, [key]: value }));
  const required = [["title", "firstName", "lastName", "birthDate"], ["whatsappNumber", "email", "residentialAddress"], ["password", "confirmPassword"]] as const;
  const canContinue = signup && required[step].every((key) => Boolean(values[key]));

  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setBusy(true); setError(""); setMessage(""); setVerificationUrl("");
    try {
      let response: Response;
      if (signup) {
        const form = new FormData();
        Object.entries(values).forEach(([key, value]) => { if (key !== "profilePhoto" && value) form.append(key, value); });
        if (values.profilePhoto) form.append("profilePhoto", values.profilePhoto);
        response = await fetch(`${apiBase}/api/teachers/register`, { method: "POST", body: form });
      } else response = await fetch(`${apiBase}/api/teachers/login`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(login) });
      const data = await response.json(); if (!response.ok) throw new Error(data.error || "We could not complete that request.");
      if (signup) { window.location.assign(`/teacher/verify?whatsappNumber=${encodeURIComponent(values.whatsappNumber)}`); }
      else { saveTeacherSession({ ...data.teacher, sessionToken: data.sessionToken } as TeacherSession); window.location.assign("/account/overview"); }
    } catch (reason) { setError(reason instanceof Error ? reason.message : "Please try again."); } finally { setBusy(false); }
  }
  const field = (key: keyof Values, label: string, type = "text", optional = false) => <label>{label}<input type={type} value={typeof values[key] === "string" ? values[key] : ""} required={!optional} onChange={(event) => update(key, event.target.value as never)} /></label>;

  return <main className="teacher-auth-page"><ParentPhotoSlider audience="teacher" /><section className="teacher-auth-main">
    <header className="teacher-auth-top"><Link href="/"><FiArrowLeft /> Back</Link><span>Teacher access</span></header>
    {signup && <nav className="teacher-auth-progress" aria-label="Teacher registration progress">{steps.map((label, index) => <span className={index < step ? "done" : index === step ? "active" : ""} key={label}><i>{index < step ? <FiCheck /> : index + 1}</i>{label}</span>)}</nav>}
    <section className="teacher-auth-card"><p className="teacher-auth-eyebrow">TribePetra Kids</p><h1>{signup ? "Join the TPK Team Portal" : "Welcome back"}</h1><p className="teacher-auth-intro">{signup ? "Create your account to access your TribePetra Kids assignments and tools." : "Sign in with your email address or WhatsApp number."}</p>
      <form className="teacher-auth-form" onSubmit={submit}>{signup ? <>
        {step === 0 && <><ProfilePhotoEditor value={values.profilePhoto} onChange={(profilePhoto) => update("profilePhoto", profilePhoto)} /><label>Title<select required value={values.title} onChange={(event) => update("title", event.target.value as Values["title"])}><option value="" disabled>Select title</option><option>Aunty</option><option>Uncle</option></select></label><div className="teacher-auth-grid">{field("firstName", "First name")}{field("lastName", "Last name")}</div>{field("birthDate", "Date of birth", "date")}</>}
        {step === 1 && <><label>WhatsApp Number<input type="tel" required value={values.whatsappNumber} onChange={(event) => update("whatsappNumber", event.target.value)} placeholder="0809 866 6128" /></label><label>Mobile Number<input type="tel" value={values.mobileNumber} onChange={(event) => update("mobileNumber", event.target.value)} placeholder="0809 866 6128" /><small>Leave blank if same as WhatsApp number.</small></label>{field("email", "Email Address", "email")}<label>House Address<textarea name="residentialAddress" required value={values.residentialAddress} onChange={(event) => update("residentialAddress", event.target.value)} placeholder="House number, street, area, city" /><small>Please enter your full residential address.</small></label></>}
        {step === 2 && <><label>Password<span className="teacher-password-field"><input type={showPassword ? "text" : "password"} minLength={8} required value={values.password} onChange={(event) => update("password", event.target.value)} /><button aria-label={showPassword ? "Hide password" : "Show password"} onClick={() => setShowPassword((shown) => !shown)} type="button">{showPassword ? <FiEyeOff /> : <FiEye />}</button></span><small>Use at least 8 characters.</small></label><label>Confirm Password<input type={showPassword ? "text" : "password"} minLength={8} required value={values.confirmPassword} onChange={(event) => update("confirmPassword", event.target.value)} /></label></>}
        <div className="teacher-auth-actions">{step > 0 && <button className="teacher-auth-secondary" type="button" onClick={() => setStep((current) => current - 1)}><FiArrowLeft /> Back</button>}{step < 2 ? <button className="teacher-auth-submit" disabled={!canContinue} type="button" onClick={() => setStep((current) => current + 1)}>Continue <FiArrowRight /></button> : <button className="teacher-auth-submit" disabled={busy || !canContinue} type="submit">{busy ? "Creating account..." : "Create account"}</button>}</div>
        </> : <><label>Email or WhatsApp Number<input required value={login.identifier} onChange={(event) => setLogin({ ...login, identifier: event.target.value })} /></label><label>Password<span className="teacher-password-field"><input type={showPassword ? "text" : "password"} required value={login.password} onChange={(event) => setLogin({ ...login, password: event.target.value })} /><button aria-label={showPassword ? "Hide password" : "Show password"} onClick={() => setShowPassword((shown) => !shown)} type="button">{showPassword ? <FiEyeOff /> : <FiEye />}</button></span></label><Link className="forgot-password" href="mailto:admin@petrachurch.ng?subject=TPK%20password%20reset">Forgot Password?</Link><button className="teacher-auth-submit" disabled={busy} type="submit">{busy ? "Signing in..." : "Sign in"}</button></>}
        {error && <p className="teacher-auth-error" role="alert">{error}</p>}{message && <p className="teacher-auth-message" role="status">{message}</p>}{verificationUrl && <a className="development-link" href={verificationUrl}>Open the development verification link</a>}
      </form><p className="teacher-auth-switch">{signup ? "Already registered?" : "Need an account?"} <Link href={signup ? "/teacher/login" : "/teacher/sign-up"}>{signup ? "Sign in" : "Register"}</Link></p>
    </section>
    <style jsx>{`.teacher-auth-page{min-height:100vh;display:grid;grid-template-columns:minmax(360px,44vw) 1fr;background:#fffdf9;color:#091936}.teacher-auth-page :global(.parent-photo-slider){position:fixed;inset:0 auto 0 0;width:min(44vw,710px);overflow:hidden;background:#11241c;color:#fff}.teacher-auth-main{grid-column:2;padding:33px clamp(28px,7vw,106px) 46px}.teacher-auth-top{display:flex;justify-content:space-between;color:#566b91;font-size:13px}.teacher-auth-top a{display:flex;align-items:center;gap:7px;color:inherit;text-decoration:none;font-weight:700}.teacher-auth-progress{width:min(100%,650px);display:grid;grid-template-columns:repeat(3,1fr);margin:36px auto 0}.teacher-auth-progress span{display:grid;justify-items:center;gap:7px;color:#a0aabd;font-size:11px;text-align:center}.teacher-auth-progress i{display:grid;place-items:center;width:31px;height:31px;border:1px solid #d8deea;border-radius:50%;font-style:normal}.teacher-auth-progress .active{color:#ef4b28;font-weight:800}.teacher-auth-progress .active i{border-color:#ef4b28;background:#fff0eb}.teacher-auth-progress .done{color:#078d61}.teacher-auth-progress .done i{border-color:#078d61;background:#e7f8ef}.teacher-auth-card{width:min(100%,650px);margin:35px auto 0;padding:34px 40px;background:#fff;border:1px solid #e9e3da;border-radius:14px;box-shadow:0 14px 45px #16253c0d}.teacher-auth-eyebrow{margin:0 0 8px;color:#e54829;text-transform:uppercase;font-size:10px;font-weight:900;letter-spacing:1.4px}.teacher-auth-card h1{margin:0;font:700 34px/1.08 Georgia,serif;letter-spacing:-.8px}.teacher-auth-intro{margin:10px 0 24px;color:#697083;font-size:13px;line-height:1.5}.teacher-auth-form{display:grid;gap:15px}.teacher-auth-form label{display:grid;gap:7px;color:#4c5361;font-size:11px;font-weight:800}.teacher-auth-form small{color:#687184;font-size:10px;font-weight:600}.teacher-auth-form input,.teacher-auth-form select{width:100%;height:44px;border:1px solid #cfd7e3;border-radius:9px;background:#fff;color:#10213d;font:13px var(--font-body);padding:0 12px;outline-color:#ff5d34}.teacher-auth-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.teacher-password-field{position:relative;display:block}.teacher-password-field input{padding-right:42px}.teacher-password-field button{position:absolute;right:8px;top:50%;display:grid;place-items:center;border:0;background:transparent;color:#60708a;transform:translateY(-50%);cursor:pointer}.teacher-auth-actions{display:flex;justify-content:space-between;gap:12px;margin-top:4px}.teacher-auth-submit,.teacher-auth-secondary{height:44px;border:0;border-radius:8px;padding:0 17px;display:inline-flex;align-items:center;justify-content:center;gap:8px;font:800 12px var(--font-body);cursor:pointer}.teacher-auth-submit{margin-left:auto;background:#ff5d34;color:#fff}.teacher-auth-secondary{background:#fff;color:#566b91;border:1px solid #d8deea}.teacher-auth-submit:disabled{background:#f4b5a5;cursor:not-allowed}.teacher-auth-error,.teacher-auth-message{margin:0;padding:11px 12px;border-radius:8px;font-size:12px}.teacher-auth-error{background:#fff0ed;color:#c53d24}.teacher-auth-message{background:#e7f8ef;color:#08734e}.teacher-auth-switch{margin:20px 0 0;color:#697083;font-size:12px;text-align:center}.teacher-auth-switch a,.forgot-password,.development-link{color:#e54829;font-weight:800}.forgot-password{font-size:11px;text-decoration:none}.development-link{font-size:11px}.teacher-auth-page :global(.parent-photo-slider-copy h2){font-family:Georgia,serif}.teacher-auth-page :global(.profile-photo-editor){display:grid;gap:11px;padding:16px;border:1px solid #dfe5ef;border-radius:13px;background:#fbfcff}.teacher-auth-page :global(.profile-photo-editor-heading){display:flex;align-items:start;justify-content:space-between;gap:12px}.teacher-auth-page :global(.profile-photo-editor-heading span){display:block;color:#13254a;font-size:12px;font-weight:900}.teacher-auth-page :global(.profile-photo-editor-heading small){display:block;margin-top:4px}.teacher-auth-page :global(.profile-photo-editor-content){display:flex;align-items:center;gap:17px}.teacher-auth-page :global(.profile-photo-preview){display:grid;flex:0 0 auto;place-items:center;width:104px;height:104px;overflow:hidden;border:3px solid #fff;border-radius:50%;background:#e8f4ed;box-shadow:0 0 0 1px #cfd9e8;color:#0a8d64;font-size:20px;font-weight:900}.teacher-auth-page :global(.profile-photo-preview img){width:100%;height:100%;object-fit:cover;transform-origin:center}.teacher-auth-page :global(.profile-photo-controls){display:grid;flex:1;gap:9px}.teacher-auth-page :global(.profile-photo-file-input){position:absolute;width:1px!important;height:1px!important;padding:0!important;border:0!important;overflow:hidden;clip:rect(0 0 0 0);white-space:nowrap}.teacher-auth-page :global(.profile-photo-choose),.teacher-auth-page :global(.profile-photo-remove),.teacher-auth-page :global(.profile-photo-apply){width:max-content;border-radius:8px;font:800 11px var(--font-body);cursor:pointer}.teacher-auth-page :global(.profile-photo-choose){height:36px;padding:0 13px;border:1px solid #e95b39;background:#fff;color:#e54829}.teacher-auth-page :global(.profile-photo-remove){border:0;background:transparent;color:#d8472a;text-decoration:underline}.teacher-auth-page :global(.profile-photo-adjustments){display:grid;gap:7px}.teacher-auth-page :global(.profile-photo-adjustments label){grid-template-columns:100px 1fr;align-items:center;color:#62708a;font-size:10px}.teacher-auth-page :global(.profile-photo-adjustments input[type=range]){height:auto;padding:0;accent-color:#ed5733}.teacher-auth-page :global(.profile-photo-apply){height:33px;padding:0 12px;border:0;background:#edf6f1;color:#078d61}.teacher-auth-page :global(.profile-photo-error){margin:0;color:#c53d24;font-size:10px;font-weight:700}@media(max-width:980px){.teacher-auth-page{display:block;padding:0 9px}.teacher-auth-page :global(.parent-photo-slider){position:relative;width:100%;min-height:225px;border-radius:0 0 22px 22px}.teacher-auth-main{padding:20px 10px 35px}.teacher-auth-card{margin-top:24px;padding:25px 20px}}@media(max-width:560px){.teacher-auth-grid{grid-template-columns:1fr}.teacher-auth-card h1{font-size:29px}.teacher-auth-page :global(.profile-photo-editor-content){align-items:flex-start}.teacher-auth-page :global(.profile-photo-preview){width:88px;height:88px}.teacher-auth-page :global(.profile-photo-adjustments label){grid-template-columns:1fr;gap:3px}}`}</style>
  </section></main>;
}
